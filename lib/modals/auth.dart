import 'package:firebase_auth/firebase_auth.dart';

class Auth {
  FirebaseAuth _auth = FirebaseAuth.instance;

  Future signup(
    String email,
    String password,
    String gender,
    int phoneNumber,
  ) {
    // _auth.
  }
}
